# Documentation index

- `design.md`: why a separate card, why the code lives only in element memory, and
  the action mapping.
- `operations.md`: build gate, release path, and the GitHub App the bump PR needs.
- `decisions.md`: dated decisions with the alternative rejected and why.
- `backlog.md`: dated open items.
