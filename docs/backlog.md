# Backlog

- 2026-09-11: a visual editor (`getConfigElement`) for the card options.
- 2026-09-11: optional haptic feedback through the Companion app's
  `haptic` event on key press.
- 2026-09-11: a dev harness with a mock `hass` like ha_card_music has, so the
  card can be exercised in a browser without an instance.
